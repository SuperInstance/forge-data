[package]
name = "forge-data"
version = "0.1.0"
edition = "2021"
description = "Structured data decomposition into tiles for Plato agents"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }
